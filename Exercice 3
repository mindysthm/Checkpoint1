1 - Pour lister les utilisateurs: etc/passwsd
2 - La commande pour changer les droits est chmod +
3- 
4 - 
5 - Pour afficher ce texte, il faut ouvrir un fichier nano en tapant nano dans le terminal. Une page s'ouvre, il faut écrire tout ce texte et quitter en enregistrant
le fichier sous un nom par exemple "texte". Pour afficher le texte dans le terminal il faut faire cat texte
6 -
7 - Le matériau réseau présent sur la couche 2 est la liaison qui gère le controle d'erreur. Sur la couche 3 c'est le réseau qui gère l'interconnexion des réseaux.
8 - Pour cd c'est Set-Location, pour cp c'est Copy-Item, pour mkdir c'est New-Item -Path, pour ls c'est Get-ChildItem.
9-
10 - Les classes IP ont été remplacées par le CIDR car avec cette ancienne méthode il manquait d'adresses IP. Le CIDR permet d'avoir beaucoup plus d'adresses IP.
